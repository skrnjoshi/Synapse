# Production Environment Variables for Render.com
# Copy these to your Render environment variables

NODE_ENV=production
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/devconnector?retryWrites=true&w=majority
JWT_SECRET=generate_a_strong_secret_here
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_SECRET=your_github_oauth_secret
PORT=10000

# To generate a strong JWT secret, run:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# MongoDB Atlas URI format:
# mongodb+srv://<username>:<password>@<cluster-name>.<random-string>.mongodb.net/<database>?retryWrites=true&w=majority

# GitHub OAuth Setup:
# 1. Go to GitHub Settings > Developer settings > OAuth Apps
# 2. Create new app with:
#    - Homepage URL: https://your-app-name.onrender.com
#    - Callback URL: https://your-app-name.onrender.com/api/auth/github/callback
